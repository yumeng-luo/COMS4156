"use strict";

var _createGoogleMapsMock = _interopRequireDefault(require("../createGoogleMapsMock"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

describe('createGoogleMapsMock', function () {
  var googleMaps;
  beforeEach(function () {
    googleMaps = (0, _createGoogleMapsMock["default"])();
  });
  it('should create a map mock', function () {
    var mapDiv = document.createElement('div');
    new googleMaps.Map(mapDiv);
    expect(googleMaps.Map).toHaveBeenCalledTimes(1);
    expect(googleMaps.Map.mock.instances.length).toBe(1);
    expect(googleMaps.Map).toHaveBeenLastCalledWith(mapDiv);
  });
});